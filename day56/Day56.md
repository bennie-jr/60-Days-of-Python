### DAY 56: Build a Food Order Management Web App with Django Python (Part 2) 

Today, I am still working on web development. I continued building app 16 which is a Restaurant Menu Management App built with django.

Recap on what I did yesterday. I started building the Restaurant Menu Management app. I started by creating a QR Code for the app. I imported the qrcode module and pillow as a dependency. Next, I proceeded to initialize and set up the Django Project and the app. The last thing I did today was create the database model. I created a class called Items and added the database schema for the table. I also added a User to the table which would serve as a foreign key with a many to one relationship. I then made the database migrations and the table was created out of my defined schema.

Today, I worked on views. In my previous Django projects the views.py consisted of functions that helped me get the database objects and render the html. This time I used a class based views where i moved from functions to classes. I wrote two classes to correspond with the 2 html pages for the app and classes uses specific variables and a sepcific way to render html pages with template_name variable so I implemented that. I also used a queryset variable to order the data that would be on the webpage(menu) by date created. Next, I created the html pages in a templates folder in the root directory and called the folder in the dir list in the settings.py, this gave me a deeper understanding on how django works. I also created urlpatterns in the urls.py file I created to add the path to the html files, I had to use as_view method because of the class based views I was using in views.py for django to recognize the html file. I also added a path in the urlpatterns inside the urls.py in the project folder which routed to the urls.py in the app folder. Finally I worked with context in django with a simple example to understand how to render data in the django to appear on the frontend. That is it for today, I will finish this app tomorrow.

Day 56 was good, I am learning more and more each day. I will continue building the app tomorrow.
