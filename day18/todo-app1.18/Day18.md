### DAY 18 - APP1: Build a To-do App with Python(Gui Configuration)

Today, I picked up from where I left off yesterday.

Recap on yesterday's code, I added functionality to the GUI(frontend) by connecting the backend i.e functions to get and write todos. 
I was able to achieve this by  starting with a while loop to keep the application running and used a match case statement to add 
functionality to the Edit todos and Add todos buttons. Today , I continued to work on the backend by implementing and adding 
functionality to the complete and exit buttons. I updated the match case statement with Complete and Exit events to remove a selected 
todo with a click of the complete button and to exit the program with the click of the exit button. I am basically done with the todo app
I added some final touches to the program like a change of theme to the UI and a clock displaying the current time. I also handled errors
with the complete and edit buttons by using the try and except block to catch the error and display a popup window asking the user to 
select an item.

I also completed a coding exercise which required adding a black theme and adding an exit button which quits the program. This was an
update to the feet inches to meter convertor program I have been writing for the past two days.

In today's bonus section I did the opposite file compression gui program I finished yesterday. I wrote an archive extractor program
which extract zip files and drops them in a destination folder. The code basically followed the same logic I used in writing the 
file compression gui program.

I did an exercise on bug fixing.

Lastly, I learned something exciting today. I was able learn and create a standalone executable for the todo app. I was able to generate
a linux executable since my os is ubuntu. This file can be found in the [Dist](./dist/gui) folder in this repository.

All in all, day 18 was good, I learned something new that I did not know till today.
I am basically done building the to-do app, I will finish up tomorrow.