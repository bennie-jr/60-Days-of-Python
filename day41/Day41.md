### DAY 41: Build a Hotel Booking App in OOP Style with Python(Part 1)

Today, I continued build on my understanding of OOP by building app 11. This app is a hotel booking app built in OOP style. This is a CLI application and not a full blown web app. I need to start small and grasp the concept of OOP better before building big projects.

Recap on what I did yesterday. Today, I converted the functional code to OOP. I created 3 classes, the scrapper and extraction functions now methods under OOP rules went into the Event class. The email function now send method went into the Email class. The read data and store data functions now methods went into the Database class and I added an init method specifically for the connection to the database. I initialized instances of these classes and called the methods and was able to get the application working just fine. I was able to understand certain concepts in OOP like classes, objects, init and self.
Today, I satrted building the Hotel Booking app with OOP. I started by planning out the sequence I wanted the app to follow and the classes I wanted to use in the code all in OOP style of course. I had a csv file which contained hotel info which I used as my data source. I imported pandas and read the csv file. I created 2 class, Hotel class and ReservationTicket class. I had 3 methods in the Hotel class, the init method , the book method and the available method. The book method booked the hotel by using the hotel id provided by the user to change its availability from yes to no and the available method checked if the hotel was available. The reservationTicket method had two methods, the init method and the generate method which generates a booking ticket for the user. I then implemented the methods of the hotel and reservation ticket classes and the logic worked like this, the code asks the user to input the id of the hotels available and the app checks if the hotel is available. If the hotel is available it then books the hotel, asks for the users details like name and uses it to generate the reservation ticket. If the hotel isn't available the user gets a prompt. I was able to get the application to work like I wanted to. I will continue adding more features tomorrow.

Also, I did a [side-project](./SP-OOP-Articles-PDF-Receipt/) where the app asks the user to enter the id of an article to purchase out of the available articles and if the article is available the user gets a pdf receipt of purchase. I used the OOP and followed a similar logic like what I used in the main app. I got the app to work like I wanted it to.

Finally, I learned more about class and instance in python.

All in all, day 41 was good, I am learning more and more each day. I will continue building app 11 tomorrow.
