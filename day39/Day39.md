### DAY 39: App 10 - Web Scraping Music Events with Python(Part 2)

Today, I completed app number 10 which is Web Scraping and  Alert App. This is a web scrapper that scrapes a website for music events and sends an email when a new event comes up.

Recap on what I did yesterday. I started app number 10 which is a web scrapper. I imported requests and selectorlib modules and I wrote  a couple of functions. The scrapper function worked by sending requests to the url of the webpage the scrapping will be happening on and converts the response to text. The extraction function uses the selectorlib method to extract that particular line of html code of the webpage that has the music events. Next I wrote an email function which sends an email alert when a new music event comes up. I also wrote two more function to read and write into the local storage. Today, I moved from file storage to a sqlite3 database. Before I could work with databases, I had to learn about SQL databases and the syntax and how to connect to your database. I created a database and added a table with the fields band,city and date. I changed the code in the data store function to insert the scraped data into the database by the fields in the data. I also changed the code in the read data function to fetch the data from the database based on fields I provided. The app worked after I tested the code and I was able to complete the app.

Also, I made a modification to the [side-project](./SP-Scrape-Temperature-Data/) from yesterday where I moved from file storage to a database. I created a database and changed the data store function to insert the temperature value that was scraped from the website together with the timpestamp of that moment into the database. I also changed the streamlit webapp to get the values of the graph(date and temperature) from the scraped data stored in the database. The modifications I made worked after testing it and I was able to complete my task.

All in all, day 39 was good, I am learning more and more each day.
