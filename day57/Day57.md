### DAY 57: Build a Food Order Management Web App with Django Python (Part 3) 

Today, I am still working on web development. I finished building app 16 which is a Restaurant Menu Management App built with django.

Recap on what I did yesterday. I worked on views. I wrote two classes to correspond with the 2 html pages for the app and classes uses specific variables and a sepcific way to render html pages with template_name variable so I implemented that. I also used a queryset variable to order the data that would be on the webpage(menu) by date created. I also created urlpatterns in the urls.py file I created to add the path to the html files, I had to use as_view method because of the class based views I was using in views.py for django to recognize the html file.

Today, I picked up from where I left off from yesterday. I created an admin interface and added data. SInce this application doesnt accept user data but rather the admin updates the app with the menu available, the data has to be put in admin page so that it displays on the home page. I created a class in the admin.py and which inherited from django admin and I added fields like list display, filter and search fields. I also had to import the models into the admin.py and called it with the admin.site.register method. Next I wrote jinja for loops and if conditionals in the index.html to grab the data that the admin through the get_context_data function in the views.py. These iterations helped display the menu on the main page after the admin updates it from the admin page. I also added dynamic links for all the meals so that when clicked on it will redirect you to a page that has the details of that meal. Lastly I added bootsrap for some styling, a base.html page and an about page. I was able to display the menu item details when clicked on it in a separate page by adding a object primary key pk in the urlspattern in the urls.py to be able to do that and this is because I was using a generic detailview in the class i created in the views.py for menu item details. I was able to complete the app and it works.

Day 57 was good, I am learning more and more each day.
