### DAY 19 - APP1: Build a To-do App with Python(Web Apps)

Today, I added the final touches to the to-do app.

Recap on yesterday's code, I continued to work on the backend by implementing and adding functionality to the complete and exit buttons.
 I added some final touches to the program like a change of theme to the UI and a clock displaying the current time. I also handled errors
with the complete and edit buttons by using the try and except block to catch the error and display a popup window asking the user to 
select an item. Today, I made the todo app into a web app. So up until yesterday I had two different versions of the to-do app, a cli version
and a desktop gui(executable) version. The final thing I did to finish with this first application was to make it into a web app with streamlit.
Streamlit is a module that helps you easily build web versions for your apps. I was able to achieve this by importing streamlit and the functions
for the backend. I used streamlit methods and instances to build the user interface. I added a function to add new todo and a for loop to check and
complete a todo. The for loop to complete a todo basically worked by ticking the todo you have completed and then it disappears. I finished building
the app, tested and deployed it on streamlit cloud servers for hosting.


In today's bonus section, I built a web app that starts a users webcam, captures a photo and converts the photo to grayscale and displays it. I was able to do this 
with streamlit and it worked perfectly.

I also completed a coding exercise which required adding a new feature to the webcam Streamlit app I built in today's bonus section. I added a feature
which allows users upload an image from their computer, the app then converts the uploaded image to grayscale and displays it


Lastly, I learned how to deploy your web application to heroku and how to manage it.

All in all, day 19 was great, I have been able to complete building the to-do app. I will continue building the rest of the applications in the coming days.