### DAY 49: Build a Python Web Automation Tool (GUI) with Selenium 

Today, I built app number 13. This is a Web Automation tool built with Selenium.

Recap on what I did yesterday. I just swapped out the SQLite3 database for MySQL database and modified the database connection code to suit what MySQL requires. I installed MySQL and created a database, a table (same table format I used with SQLite3). I inserted data into the table and all I was doing with creating the database and inserting table data was to get familiar with how MySQL works. After I was done creating the database and the table, I modified the codebase to suit MySQL. I added the authentication with the database connection and then edited the SQL queries of SQLite3 to the use case of MySQL.

Today, I built a Web Automation tool with Selenium. This tool automated website login and form filling and submission on the same website given the required parameters. I wrote this code in 3 phases, I wrote the first procedural code then converted it to OOP then moved on to the GUI with selenium and tkinter. Before I could start using selenium, I had to download a chromedriver from google chrome since that is the browser I am going to use. I downloaded the chromedriver and unzipped the contents to my code root directory. I imported the selenium library with its various methods. I used the selenium driver to load the webpage. I inspected the webpage I was using to get the html element id in the html code in order for my code to use it to fill the needed parameters on the webpage. Since this tool basically loads a webpage, logs in using the user login details and fills and submits a form I need the id of every element the tool was going to interact with. After getting all the ids, I was able to write the code to login using my login details I provided. Once the login was done the tool was able to click on the form, fill it and submit the form on the website. After testing the code and making sure it works, I converted the code I had written to OOP and after I used tkinter for the GUI interface. The GUI interface took the user input and I didnt have to hard code all the input fields details in the code anymore. I was able to complete this code and it worked like it was supposed to. 


Day 49 was good, I am learning more and more each day.
