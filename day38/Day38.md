### DAY 38: App 10 - Web Scraping Music Events with Python(Part 1)

Today, I started building app number 10 which is Webcam Alert App. This is a web scrapper that scrapes a website for music events and sends an email when a new event comes up.

Recap on what I did yesterday. I worked on the emailing part which sent an email with a picture attachment of the moving object in the frame. I imported the smtplib module and wrote a function with an argument that takes the image_path of the image to be sent and uses it as an attachment before the email is sent. I also wrote a function which cleaned(deleted) all the images in the images folder once the email had been sent. The program worked like I wanted it to and the emails were sent successfully. App number 9 was completed. Today, I started app number 10 which is a web scrapper. I focused on getting the scraper to work and the emailing as well. I imported requests and selectorlib modules and I wrote  a couple of functions. The scrapper function worked by sending requests to the url of the webpage the scrapping will be happening on and converts the response to text. The extraction function uses the selectorlib method to extract that particular line of html code of the webpage that has the music events. But before the extraction is done I had to create an extract.yaml file which I placed the id of the line of that hmtl (whether its an h1 header etc) in and that  is what the selectorlib uses to get the music event on the webpage. Next I wrote an email function which sends an email alert when a new music event comes up. I also wrote two more function to read and write into the local storage. I am not using mysql yet so I created a txt to store the music events. The whole logic of this application works like this, scrape the webpage and extract the music events and if the string extracted shows "no upcoming tours" ignore. If there is an actual music event extracted write it into the txt file and send an email. When another scraping is done check the event against the events in the txt local storage if it doesnt match any send an email alert. I was able to get the application working like I wanted it to. I will work on the database part of the application tomorrow with mysql.

Also, I did a [side-project](./SP-Scrape-Temperature-Data/) where I scrape temperature data from a webpage, extract the number and append it to a txt file with a timestamp making it a csv file with a comma separator between the time stamp and temperature. I also wrote a streamlit app that plotted a graph of date against temperature from the data I collected into the txt file. The application worked well although I could have used a better solution in extracting the lines in the txt file for the graph.

Finally, I learned about the concept of web scraping and how it works.

All in all, day 38 was good, I am learning more and more each day. I will finish building app number 10 tomorrow.
